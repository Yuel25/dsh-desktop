# dsh-desktop 优化计划

审查日期：2026-09-02  
状态：核心改造已实施，以下保留原方案及验收要求；具体验证范围见修复记录与发布验证记录。

### 2026-09-03 审查修复记录

发布前复查补充：修复已关闭窗口的后台清理尚未完成时应用提前退出的问题，回归测试增至 17 项。合并上游依赖更新后，测试加载器改用 esbuild 适配 TypeScript 7。类型检查、回归测试和生产构建通过；Electron 44 中使用隔离配置和模拟本地服务验证了实际打包内容的启动、设置页面、语言与标题栏切换及桥接隔离。真实 DSH 故障注入和安装向导交互测试仍未覆盖。

针对实施后的四项审查问题，已补齐恢复启动前后的取消检查及失败清理、持续写入时的日志轮转、设置原子保存与失败回滚，并将复制业务逻辑的测试替换为直接加载生产 TypeScript 模块的测试。测试仅替换 Electron、进程和网络等环境依赖。

本轮验证：16 项回归测试、主进程与界面类型检查、生产构建通过。覆盖恢复时关闭窗口、超时清理、启动错误、重复打开、profile 回滚、外部进程保护、日志轮转及写入失败、设置保存失败和导航校验。尚未进行真实 Windows 桌面故障注入；该记录不表示原计划全部验收项已经完成。

## 目标与范围

优先解决进程生命周期、安全边界和异常反馈问题，再完善日志管理和模块结构。保留当前设置页的视觉设计、中英双语和已有功能。

上一轮已完成的修复包括：独立 profile 窗口首次显示、重启时清理独立服务、区分运行端口与配置端口、等待 profile 切换完成后返回设置。这些行为需要在后续修改中保持。

本轮发现主要来自代码审查；重复打开窗口、导航正则绕过、缺失命令提示匹配等路径已做模拟验证，尚未完成真实桌面环境的故障注入验证。

## 实施顺序

| 阶段 | 内容 | 完成条件 |
| --- | --- | --- |
| 第一阶段 | 导航与外链校验、子进程启动错误处理、失败清理与启动取消、重复打开请求合并 | 安全边界和进程生命周期回归验证通过 |
| 第二阶段 | DSH 安装检测、设置操作反馈、更新超时 | 失败原因明确，界面不会错误提示成功或持续等待 |
| 第三阶段 | 日志轮转、模块拆分、CI 完善 | 日志占用有上限，核心逻辑可独立测试 |

测试随各阶段一起补充，第三阶段负责整理并完善 CI，不把前两阶段的验证推迟到最后。

## 第一阶段：安全与进程稳定性

### 1. 精确校验页面导航及外链（P1）

**现状**：`src/main/index.ts` 的 `attachRendererGuards()` 使用正则前缀识别本地服务。`http://127.0.0.1:3080@example.com/` 可以匹配，但实际主机是 `example.com`。窗口打开处理器还直接将任意 URL 传给 `shell.openExternal()`。

**方案**：

- 用 `new URL()` 解析地址，精确比较协议、主机和端口，拒绝带用户名或密码的服务 URL。
- 每个窗口只允许访问其对应的 DSH 服务来源，以及应用明确指定的本地页面；开发环境单独允许准确的开发服务器来源。
- 本地页面按实际文件路径验证，不把所有 `file:` 页面视为可信。
- 统一外链打开入口，默认仅允许 HTTP/HTTPS；其他协议按明确的产品需求逐项添加。
- 同步检查重定向路径，以及 IPC 调用的实际发送 frame，避免只验证顶层窗口 URL。

**验收**：合法主服务与独立窗口导航正常；用户名伪装、错误端口、非预期本地文件和非允许协议被拒绝；常规文档链接仍由系统浏览器打开。

### 2. 补齐子进程启动错误与日志流错误处理（P1）

**现状**：`spawnExtraProcess()` 监听了 `exit`，但没有监听 `error`。启动器不存在或无法执行时，错误可能未被处理。

**方案**：

- 为主服务和独立服务统一处理启动失败、进程退出和日志写入失败。
- 将启动错误交给就绪等待流程，尽快结束等待并给出提示。
- 日志流关闭和进程清理应可重复调用，避免重复报错或错误触发恢复。
- 明确区分主动停止、启动失败和运行中崩溃。

**验收**：模拟启动器不存在、权限错误和日志写入错误，应用主进程不崩溃；失败窗口得到明确反馈，相关资源被释放。

### 3. 启动超时先清理，再重试或回滚（P2）

**现状**：profile 切换或恢复启动超时后，子进程可能仍存活；直接重试可能占用原端口或覆盖进程引用。

**方案**：

- 为每次启动保留独立的进程引用和操作标识。
- 启动失败后，先终止本次操作启动的进程树并等待清理完成，再执行重试或恢复旧 profile。
- 旧进程的退出回调只更新其所属实例，不能清空新进程的引用。
- 就绪等待支持取消；关闭窗口、退出应用或重启时，取消未完成的启动。
- 在异步等待后、创建进程前再次检查取消或退出状态。
- 用明确的状态管理启动、运行、停止和恢复，避免多个布尔状态组合产生竞态。
- 始终保留进程所有权边界，不终止外部启动的 DSH。

**验收**：模拟服务迟迟不就绪、切换失败、启动时关闭窗口和恢复时退出；不遗留自有进程、不误杀外部进程、不由旧回调覆盖新状态。

### 4. 合并同一 profile 的并发打开请求（P2）

**现状**：`openProfileWindow()` 在检查已有窗口之后，异步分配端口，再登记实例；并发请求可能创建两个窗口和服务。

**方案**：

- 按 profile 保存正在进行的打开 Promise，后续请求复用同一次操作。
- 操作失败或完成后移除进行中记录，保证可以再次尝试。
- 对已打开窗口继续复用并聚焦。
- 设置页显示打开中状态，避免用户误以为点击没有生效。

**验收**：同一 profile 连续或并发点击只产生一个实例；不同 profile 可以同时打开；首次失败后可以重试。

## 第二阶段：安装检测与交互反馈

### 5. 可靠识别 DSH 未安装（P2）

**现状**：子进程退出被转换为通用错误文本，`looksLikeDshMissing()` 无法据此区分命令缺失与普通启动错误。

**方案**：

- 在启动前解析可用 DSH 命令或启动器，避免完全依赖本地化错误文本。
- 使用结构化错误区分未安装、不可执行、启动退出和就绪超时。
- 只有命令确实缺失时显示安装引导；其他错误展示相应原因及日志入口。
- 点击重试时重新检测，避免缓存此前的缺失结果。

**验收**：无 DSH、正常安装、启动器失效和 profile 配置错误分别进入正确的提示流程；安装后可以重试成功。

### 6. 统一设置操作结果与更新超时（P2）

**现状**：设置请求正常返回即显示“已保存”，但 profile 可能已经回滚；开机自启、复制诊断等操作缺少统一错误反馈；更新请求没有应用层超时。

**方案**：

- 统一处理进行中、成功和失败状态，并恢复按钮可用状态。
- profile 切换根据实际返回状态判断是否达成目标；回滚或拒绝切换不能显示成功。
- 为开机自启、日志读取、打开窗口和复制诊断等操作提供失败提示。
- 更新请求使用可取消的超时机制，建议初始超时为 15 秒；前端通过 `finally` 恢复按钮。
- 保留未保存的端口草稿，避免无关设置刷新覆盖用户输入。
- 在中英文中提供一致的提示含义与可访问状态通知。

**验收**：模拟切换回滚、IPC 拒绝、剪贴板失败和更新网络超时；提示与实际结果一致，按钮不会一直禁用，端口草稿不会被无关操作覆盖。

## 第三阶段：长期维护

### 7. 有上限的日志保留

- 为主服务、各 profile 及恢复日志提供轮转机制。
- 建议起始策略：单文件 5 MiB，保留当前文件和 3 份历史文件；另设日志目录总量预算，避免 profile 数量增加后总占用失控。
- 优先清理旧的已轮转文件，不直接删除仍在写入的日志。
- 日志查看器继续支持尾部读取，并能辨别当前日志与历史日志。
- 验证连续写入、轮转和读取并发时不会崩溃或中断服务。

### 8. 按职责拆分模块

建议逐步拆分以下职责，具体目录在实施时依据依赖关系确定：

- 服务管理：命令发现、启动、停止、取消、恢复和进程所有权。
- 窗口管理：主窗口、设置窗口、独立窗口及生命周期。
- 安全校验：导航、外链和 IPC 来源。
- 设置存储：校验、迁移、持久化和运行配置。
- 日志与更新：文件轮转、诊断收集、版本检查。
- 设置界面：导航、各设置面板、异步操作反馈与本地化文本。

先在修复中提取需要测试的边界，再逐步拆分；不将大规模重构与所有行为变更混在一起。同步清理未使用变量和过长的 JSX 表达式。

### 9. 回归测试与 CI

- 将生命周期测试加入仓库，使用可注入的进程、网络和计时依赖，避免测试启动真实 DSH 或修改用户配置。
- 优先覆盖：启动错误、超时清理、取消、重复打开、恢复回滚、重启清理和外部进程保护。
- 为 URL 校验添加表驱动测试，覆盖用户名伪装和协议边界。
- CI 在类型检查和生产构建之外执行回归测试。
- 发布前补充真实 Windows 冒烟验证：首次启动、托盘关闭、独立窗口、重启、退出、安装引导和最小窗口尺寸。

## 交付标准

- 各阶段都有对应的行为验证，不只以编译通过作为完成依据。
- 保留之前已修复的行为，并更新中英文说明。
- 实施过程中不修改用户 DSH 配置，不清理外部服务进程。
- 最终报告区分自动化验证、模拟验证、真实桌面验证和仍未覆盖的情形。
